# Quarto slides template

A Quarto RevealJS slides template that runs on a Docker container.

## Usage

### Preview

You can use *Docker* to **preview** the slides by running the following command:

```bash
docker compose up --build
```
